import './music_companion';

// Export any necessary objects for SillyTavern extension system
// The plugin is self-initializing through bootstrapPlugin()
// and exposes itself via window.NeteaseMusicPlugin

// If SillyTavern expects a default export, we can export a dummy object
export default {};